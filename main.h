#ifndef MAIN_H
#define MAIN_H

#include<stdio.h>
#include<stdlib.h>
#include<stdarg.h>
#include <unistd.h>

void _percent(int ch);
void _puts(const char *str);
void _putchar(int c);
int _printf(const char *format, ...);

#endif
